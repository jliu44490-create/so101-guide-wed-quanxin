# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables — never commit secrets. Keep .env.example tracked.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# Local package-manager registry override (e.g. China mirror) — keep local, don't commit
.npmrc
# TypeScript incremental build cache
*.tsbuildinfo
# Packaged cloud function (build artifact for console upload)
cloudbase/functions/*.zip

# Claude Code local config
.claude/